name: Deploy to FiveM Server

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy 🚀
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Sync files to hosting
        uses: sebastianpopp/ftp-deploy@v1.0.1
        with:
          ftpServer: root.atlasnetwork.uk
          ftpUsername: archie
          ftpPassword: ${{ secrets.ftp_password }}
          localSource: ./
          remoteTarget: ./
